import 'package:flutter/material.dart';
class MainListe extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ma liste des médicaments'),
        backgroundColor: Colors.green[800],
      ),
      body: Container(

      ),
    );
  }
}